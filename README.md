Install Dependencies:
npm init -y
npm install express-router
npm install express
npm install express cors dotenv mysql2 uuid

1. Type in the terminal
	mysql -u root -p 
2. password: 
	rootroot
3.  You can check the available databases
	SHOW DATABASES 
4. you can exit by typing 
	"exit" 